const Footer = () => {
  return (
    <footer className="w-full border-t border-red-900/30 bg-black py-8 px-8 mt-auto">
      <div className="mx-auto flex flex-col sm:flex-row items-center justify-between gap-4 text-zinc-600 text-xs tracking-widest uppercase">
        <span>Gradey Dick Fan Club</span>
        <span className="text-red-900">When will we get Hollywood back??</span>
      </div>
    </footer>
  );
};

export default Footer;